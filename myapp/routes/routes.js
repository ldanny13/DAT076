/*var express = require('express');
var router = express.Router();

//Displays our homepage
app.get('/', function(req, res) {
  res.render('menu');
});

//Displays the login page for an admin
app.get('/admin'), function(req, res){
  res.render('admin');
});

module.exports = router;*/
